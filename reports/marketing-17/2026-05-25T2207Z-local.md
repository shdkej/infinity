# marketing-17 실행 보고서 (local)

- intent: marketing-17 — Virtue 첫 세션 정성 마찰 관찰 프로토콜
- mode: execute_local
- 실행 시각: 2026-05-25T2207Z
- 실행 주체: Infinity → Claude Code (workflow-master 4역할 병렬 합성)
- 결과: **완료 (archived)**
- approval: **agent-approved L2** (target repo push 1건)

## 한 줄 요약

`virtue-rebirth-app`에 첫 세션 정성 마찰 관찰 프로토콜 문서 1개를 추가했다. J1~J4 첫 가치 경로 위에 수기 정성 마찰 태그(F1~F9), 단계별 `value-critical`/`value-adjacent`/`non-critical at activation` 분류, 첫 3명/첫 10명 검증 게이트를 신규 계측 없이 정리했다. 코드·이벤트·대시보드·카피·배포 변경 0.

## 산출물

- repo: `virtue-rebirth-app`
- branch: `master`
- commit: `2a8c694` (이전 HEAD `87b8877`에서 fast-forward)
- path: `apps/web/docs/first-session-friction-observation-protocol.md` (신규 1파일, +289 라인)
- 푸시: `87b8877..2a8c694 master -> master`, push 후 local HEAD == `origin/master` == `2a8c694`, 워킹트리 clean

## 문서 구성

- §0 목적 + 전제 (처분/기록양식/시간계산과의 경계 못박기, 신규 0, 외부 0)
- §1 왜 숫자보다 행동 증거를 먼저 보는가 (Goldcast/LogRocket + Arise GTM/Hi-Books + 세스 고딘 렌즈)
- §2 가치 경로 5단계(`/`→`/add` 입력→결과 카드→저장→`/` 복귀) + J1~J4 첫 가치 도달 매트릭스
- §3 정성 마찰 관찰 태그 사전 F1~F9 (Intent 지정 9종과 1:1)
- §4 단계별 가치-기여 분류 (잡별 value-critical/adjacent/non-critical at activation, 같은 단계가 잡별로 부호 반전)
- §5 코드 근거 (신규 0, 발화 위치는 선행 문서에 위임, `/` 캡처 미발화 → 정성 전용, `level_up_viewed` 첫 세션 미발화는 설계상 정상)
- §6 첫 3명 / 첫 10명 검증 게이트 + Failover/가용성 caveat (인프라 실패는 UX 마찰로 오태깅 금지)
- §7 운영자 사용 노트 + prelaunch 관찰 금지선
- §8 copy-spec 준수 (금지어 0, 사용자 노출 카피 0)
- §9 Intent 9항목↔F-태그 정합표 + 충돌 점검(선행 9문서)
- Out of scope + 검증 매핑

## 핵심 설계 결정

1. **렌즈 직교화로 충돌 회피.** 기존 `activation-path-friction-audit.md`(good/bad/hold 처분 렌즈)와 구분되는 *관찰 프로토콜* 렌즈로 한정. baseline-template(1인 1행 기록면)·TTV(시간 간격)·three-screen(화면 닫힘 구조)와 역할 분리. 태그 ID는 `F-` 접두로 ES-/S- 네임스페이스 충돌 회피.
2. **마찰 태그 명칭은 Intent 지정 9종과 1:1.** Marketer 안 채택(F1 반복 클릭 … F9 카피·의미 불일치). "AI 채점 대기"는 잡별 부호 반전 *처분* 대상이라 별도 태그로 만들지 않고 friction-audit에 위임, 본 문서는 J1·J4 멈춤을 F3/F5로 관찰만.
3. **선행 발견 계승.** F7(AI 약속 공백)=three-screen §3-A J3 앞단 끊김, F8+F6(누적/대시보드 payoff 공백)=§3-B J2 뒷단 누출. 잡별 이벤트 귀속(J1/J2/J4=`deed_saved`, J3=`deed_judged`, J2 보조=`level_up_viewed`) 재정의 0.
4. **하드코딩보다 구술.** 이벤트는 이름과 단계로만 참조하고 file:line은 선행 문서를 단일 출처로 위임(드리프트 방지). Developer 사실검증에서 발화 위치·612/1000 경계 드리프트 0 확인.

## 검증 게이트 결과 (전부 PASS)

| 게이트 | 결과 | 증거 |
|---|---|---|
| 변경이 `apps/web/docs/` 문서 1개로 한정 | PASS | `git status --short` → 신규 doc 1개만 |
| 코드/iOS 변경 0 | PASS | `git diff --stat -- apps/web/src apps/ios apps/ios/Sources` 빈 출력 |
| 실제 충돌 마커 0 | PASS | 라인 시작 앵커 검색 무매치. 느슨한 검색의 유일 매치는 §Out-of-scope self-check 명령(line 273) 자기참조 — 선행 marketing-16 문서와 동일 관례 |
| 신규 이벤트/속성/대시보드/코드/카피/배포/트래킹 0 | PASS | §5 신규 0 + §0 전제2 + Out of scope. `/` posthog 카운트 0 재확인 |
| 3개 지정 선행 문서 충돌 0 | PASS | §9-2 충돌 표(`first-session-jtbd-matrix`/`three-screen-value-path-audit`/`ios-activation-event-parity-brief` 모두 "계승/인용만") |
| target repo push 후 git clean | PASS | local HEAD == origin/master == `2a8c694`, 워킹트리 clean |

## L2 push 승인 근거 (agent-approved)

- 이 intent에 직접 결속(문서가 곧 산출물) · 가역(단일 doc 커밋, revert 가능) · 비용 0 · 프로덕션/시크릿/권한 변경 0 · 제3자 메시징 0 · push 전 상태(HEAD==origin) 확인 · push 후 검증(HEAD==origin) 수행. 모든 조건 충족.

## workflow-master 적용 기록

- target repo에 `.agent/workflows/workflow-master.md`·`WORKFLOW-MASTER.md` **부재** → 부재 기록 후 게이트 수동 적용.
- 복잡도 **중간**(단일 문서, 다중 출처 합성 + 선행 9문서 무충돌 필요)으로 분류. 단독 실행 대신 Planner/Developer/Marketer/Operator 4역할 Task 병렬 실행 후 workflow-master가 합성.
  - Planner: §0~§8 아웃라인 + 매트릭스/태그/분류 표 컬럼 설계 + 검증 매핑.
  - Marketer(Owner): §1 프레이밍 + F1~F9 최종 명칭/정의(Intent 9종 1:1) + copy-spec 준수 + 포지셔닝 무충돌 행.
  - Developer: 코드 사실검증(이벤트 발화 위치, `/` 캡처 부재, 612/1000 경계) 드리프트 0 + §5 초안 + self-check 명령.
  - Operator: 첫 3명/첫 10명 게이트 + 운영자 사용 노트 + Failover caveat + 무충돌 행.

## 후속 후보 (별도 Intent)

- 반복 관찰된 value-critical 단계 막힘의 *처분*(개선안) — friction-audit 렌즈 + 별도 Intent.
- iOS·`/deeds`·`/dex` 마찰 관찰 — 본 문서는 웹 첫 세션 핵심 경로 한정.
- 세션 리플레이 도구·대시보드·자동 집계 — 모두 approval-needed(Waiting).
