# Safety Map correction — Waiting

## Decision

The intent moved to **Waiting** before implementation. Its required interactive Mapbox map cannot be built within the recorded L1 boundary: existing Mapbox use is a server/local Remotion static-image workflow, the protected secret store has no entry, and a browser Mapbox GL map necessarily makes its public access token observable in client requests.

## Evidence checked

- `intents/context/safety-map-correction-20260903.json`
- `INTENTS.md` (deadline, immutable Slack origin metadata, lifecycle rule)
- `prompt-archive/BRAND.md`, `DESIGN.md`, `DESIGN_SYSTEM.md`
- `video-automation/remotion-map/prepare*.mjs` (environment/local-only token use)
- `scripts/dispatch_terminal_notifications.py` and its tests (destination-aware exactly-once terminal receipt)
- Secret-store metadata: empty; no value was requested or read.

## Role synthesis

- **Planner:** retain real place/road context only; safety incidents remain no-data; do not Archive before the deadline.
- **Developer:** browser Mapbox GL conflicts with the token non-exposure rule; a proxy expands infrastructure, cost, and permission scope.
- **Marketer:** the future map must be a location-context object, never a safety score, route recommendation, or guarantee; fixture must not overlap real geography.
- **Operator:** retain all four Slack metadata fields through any eventual Archive; terminal delivery is dispatcher-owned and exactly-once only after remote verification. The current notifier test suite confirms this contract.

## Exact resume decision

User approval is needed for exactly one architecture:

1. a domain-restricted public Mapbox token that is intentionally observable by browsers; or
2. a token-non-exposing map proxy, including its infrastructure, cost, and permission scope.

Neither choice permits fabricating safety signals. Until then, no Mapbox token, configuration, Terraform, deployment, Slack delivery, or Archive action was performed.
