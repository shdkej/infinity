# 치안 지도 3차 실험 — 종료 보고서

- Intent: `safety-map-experiment-03-20260905`
- 종료 시각: `2026-09-06T08:00:00Z`
- 종료 권한: 사용자 명시 요청 — “마감시키고 결과보고서 작성해줘”
- 종료 결과: `archived_user_closed`
- 라이브 배포본: <https://safety-map-experiment-03.aws.shdkej.com/>

## 결론

독립 전역 지도 UX의 구현·배포·라이브 검증은 완료했고 Red 최종 통과도 확보했습니다. 다만 후속으로 추가된 실제 근거 데이터 조사(T6)는 사용자 승인 지역 범위가 없어 시작하지 않았습니다. 이번 종료는 기능 실패가 아니라, 검증되지 않은 치안 데이터를 임의로 도입하지 않기 위한 사용자 명시 마감입니다.

## 완료·미완료 작업

### 완료

- T1~T5.3: Context Pack, PRD, 역할 검토, 격리 경로, 지도 캔버스·검색·확대/이동·레이어, 접근성·추적 경계, desktop/390px 확인, Red 검토, 배포·원격·Slack 영수증.
- T5.4: 이 종료 보고서와 Archive 메타데이터를 기록합니다.
- 검증 근거: `artifacts/safety-map-experiment-03-20260905/red-final-report.md`, `artifacts/safety-map-experiment-03-20260905/red-dashboard-recovery-report.md`, `reports/safety-map-experiment-03-20260905/20260905T1508Z-red-closure.md`.

### 미완료

- T6.1~T6.4: 특정 행정구역 확정, 공식 원자료·라이선스 조사, 관측/발행/갱신일·결측 검증, 표현 한계와 Red 관문.
- T6은 종료 시점에 재개하지 않습니다. 실제 위험 표시·점수·예측·경로 추천·위치 수집·실시간 사건 데이터도 도입하지 않았습니다.

## 실제 차단 원인과 근본 원인

1. 실제 근거 데이터는 어느 국가·도시를 대상으로 할지에 따라 출처, 라이선스, 공간·시간 단위와 표현 한계가 달라집니다.
2. PRD의 안전 경계는 행정구역을 임의로 정하지 못하게 합니다.
3. 따라서 지역 범위가 없는 상태에서 데이터 조사를 진행하면, 검증 범위와 사용자 의도를 임의로 확장하게 됩니다.

근거: `reports/safety-map-experiment-03-20260905/20260905T1921Z-t6-region-scope-blocker.md`.

## 시도한 대안과 증거

- 지도 기능은 실제 데이터 도입과 분리해 완성했습니다. 라이브 지도에서 Milan 검색, 기본/위성 레이어 전환, 390px 가로 넘침 부재를 확인했습니다.
- 외부 지도 SDK의 성능 측정 전송이 추적 금지 경계와 충돌한 문제는 `performanceMetricsCollection:false`와 CSP 보완 후, 새 문서 요청 검사와 Red 재검토로 해소했습니다.
- 지역 범위를 임의 지정해 T6을 진행하는 대안은 PRD 안전 경계를 위반하므로 채택하지 않았습니다.

## 재시도 가능 범위와 외부 결정

- 자율 재시도 가능: 현재 라이브 지도 UX의 유지·기술적 회귀 점검.
- 사용자/외부 결정 필요: 실제 데이터를 조사·표시할 단일 행정구역 지정, 해당 데이터의 공개·재배포 조건이 불명확할 때의 도입 승인.
- 새 범위가 승인되면 별도 Intent로 T6.1부터 재개하며, 기존 지도에 검증 전 위험 판단을 추가하지 않습니다.

## 운영·구현 개선

1. 제품 기능 완료와 데이터 도입 조사를 별도 Intent로 분리합니다. 데이터 범위가 미정일 때 제품 완료 상태를 불필요하게 유지하지 않습니다.
2. 외부 SDK는 기능 렌더만이 아니라 기본 telemetry·CSP·실제 네트워크 요청까지 함께 검증합니다.
3. 마감 보고서에는 구현 완료와 확장 범위 미시작을 명확히 분리해, 완료를 과장하거나 차단을 일반적인 Waiting으로 숨기지 않습니다.

## 지표와 다음 판단

- metric_result: 전역 지도 탐색이라는 본래 질문은 라이브 캔버스·검색·레이어·모바일·Red 검증으로 통과했습니다. 실제 치안 데이터의 정확성은 아직 평가 대상이 아닙니다.
- metric_next_decision: 현 배포본은 유지합니다. 실제 치안 데이터 도입은 사용자 지정 지역과 공식 근거·라이선스 검증이 있는 별도 작업에서만 판단합니다.

## 종료 알림 복구

- 누락 원인: Archive 항목에 `remote_verified: pass`와 원 요청 Slack 스레드의 `notification_channel`·`notification_target`·`notification_reply_to`가 빠져 dispatcher가 완료 알림 후보로 만들지 못했습니다.
- 조치: 해당 불변 원점 메타데이터와 원격 검증 상태를 복구한 뒤 terminal notifier를 실제 원 스레드에 한 번 실행합니다. receipt는 `data/dispatcher-terminal-notifications.json`에 남깁니다.
