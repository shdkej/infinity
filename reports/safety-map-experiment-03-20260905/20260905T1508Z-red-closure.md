# 치안 지도 3차 Red 폐쇄 루프

## 1차 Red 결과와 보완

- 세션: `agent:red:subagent:bf0109ad-c135-49ba-a867-ac6e9932e229`
- 판정: `RED_STATUS: fail`
- 근거: 라이브 지도·검색·레이어·390px은 통과했지만 `events.mapbox.com` 성능 측정 전송이 `NO TRACKING` 경계와 충돌했습니다.
- 보완: 지도 생성 옵션에 `performanceMetricsCollection:false`를 넣고, CSP `connect-src`에서 `events.mapbox.com`을 제외했습니다.

## 보완 배포와 라이브 증거

- Space 원격 커밋: `f3c3cb00a5f5fc2bd81d31daababbe4a26b63ab3` = `origin/main`
- 정식 배포: workflow `33962687035` 성공
- 라이브 URL: `https://safety-map-experiment-03.aws.shdkej.com/` HTTP 200
- 새 브라우저 문서의 정규화된 resource host 검사: `events.mapbox.com` 없음
- 라이브 동작: 캔버스 렌더, Milan 검색, 기본·위성 레이어 전환, 390px 가로 넘침 없음
- 값 보호: runtime 설정 값과 토큰 값은 읽거나 기록하지 않았습니다.

## 2차 Red 결과와 현재 조치

- 세션: `agent:red:subagent:f2dea086-ace6-49b2-b714-9805fe10004c`
- 판정: `RED_STATUS: fail`
- 근거: telemetry 보완과 라이브 검증은 통과했으나 당시 Infinity `origin/main`에 실행·1차 Red 기록이 동기화되지 않았습니다.
- 조치: 이 리포트, 1차 Red 리포트, task-plan, Intent 상태를 Infinity 원격에 동기화한 뒤 동일 범위의 targeted Red 재검토를 요청합니다.

## 시간 편차

- T3.3은 초기 실행 설정 차단, 승인 뒤 설정 생성·배포·라이브 검증이 포함되어 예상보다 70분 길었습니다.
- T4.4는 배포 workflow와 라이브 재검사가 2분으로 끝나 예상보다 23분 짧았습니다.
- 나머지 시간 근거는 `20260905T1245Z-leaf-timing.md`에 기록하며, 근거 없는 시간은 추정하지 않습니다.
