# T3.2 전체 화면 FocusField와 Typography Rail

## 구현

e03 전용 `dist/index.html`과 `dist/styles.css`에 full-bleed 지도 FocusField의 첫 렌더를 만들었습니다. 아직 실제 지도라고 주장하지 않도록 화면에는 `GLOBAL FIELD / 준비 중`을 표시합니다.

- Typography Rail: 목적, 하나의 검색 행동, no-data 안전 경계를 배치했습니다.
- FocusField: 전역 지도의 공간감을 위한 저대비 좌표·경로 레이어만 제공합니다.
- 모바일: 600px 이하에서 Rail·상태 라벨·footer가 겹치지 않도록 별도 규칙을 넣었습니다.
- reduced motion: 애니메이션/전환 억제 규칙을 제공합니다.

## 검증

- no-data·안전 점수 금지·FocusField 구조 문자열 확인: 통과
- token literal (`pk.`) 미검출: 통과
- legacy `sites/safety-map/**` diff 미검출: 통과
- Space 원격 SHA: `800bdddab3a2dd903ed48f5196c791cdad47a2db` (`HEAD == origin/main`)

## 제한과 다음 액션

이 leaf는 화면 구조만 구현했습니다. Mapbox canvas, place/road search, zoom/pan, basemap layer는 T3.3에서 실제 기능으로 구현·검증합니다.
