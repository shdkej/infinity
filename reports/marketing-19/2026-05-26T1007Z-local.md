# marketing-19 Local Report — Virtue 신규 사용자 홈 화면 FAE 감사표

- intent: `marketing-19`
- title: Virtue 신규 사용자 홈 화면 FAE 감사표 작성
- run: local Claude Code delegated task
- timestamp: 2026-05-26T10:07Z
- result: **완료 (completed)** — 내부 감사표 1개 작성·커밋·push, Infinity Inbox→Archive 마감

## 1. 무엇을 했나

신규 사용자가 도착하는 홈 화면 `/`이 **일반 기능 메뉴판이 아니라 First Activation Event(FAE)로 향하는 방향판인지**를 감사하는 내부 문서를 작성했다. 코드·카피·이벤트·대시보드·배포는 **하나도 바꾸지 않았다**(전부 인용·가설 평가로만 기록).

산출물: `apps/web/docs/home-screen-fae-audit.md` (신규 1파일).

- §0 목적 + 전제 4줄(prelaunch 관찰 기준·신규 0·선행 인용만·이벤트는 관찰 게이트).
- §0-1 **경계 못박기** — three-screen(경로 닫힘)·empty-state(빈 상태 카피 결정)·friction-protocol(막힘 후 관찰)과 축을 구분, 본 문서는 "진입 시 `/`가 첫 행동을 가리키나(이정표)"로 한정.
- §1 J→첫 활성화 행동 매핑(인용): **J1/J2/J4 → `deed_saved`, J3 → `deed_judged`**, setup=`add_flow_started`, J2 부가=`level_up_viewed`.
- §2 **문서의 심장** — `/`의 네 요소(주 CTA `오늘 덕 쌓기` / 빈 상태 `최근 덕행` / 최근 덕행 영역 / 누적·환생종 신호)를 세 질문("무엇을 먼저 / 왜 지금 / 하면 무엇이 생기나")으로 **J1/J2/J4 vs J3** 강·약 평가. 모든 셀은 `apps/web/src/app/page.tsx` 실제 렌더 트리(file:line) 근거.
- §3 signpost gap G1~G5(빈 덕력 숫자·경쟁 모듈 다수·J3 홈 AI 훅 부재·CTA after-state 미예고·환생종 카드 양면성).
- §4 관찰 게이트 — 이벤트 4개를 "닿음" 이진 관찰 보조로만 한정, `/`는 capture 미발화(정성 관찰 전용), 전환율 환산·닫힘 단정 금지.
- §5 Scope/Out-of-scope, §6 충돌 점검(선행 8문서 0충돌), §7 검증 매핑.

**핵심 발견:** 홈 화면은 J1/J2의 약속에 강하고(빈 상태·CTA·환생종이 저장→누적 경로를 가리킴) **J3에는 구조적으로 약하다** — J3가 찾는 AI 신호는 `/add` 진입 전까지 홈에 사실상 없고, 첫 진입엔 `ScorePill` 점수 칩조차 빈 상태라 부재. 이는 `three-screen-value-path-audit.md` §3-A(J3 앞단 끊김), positioning R3, JTBD §3 J3 메모와 모순 없이 일치한다.

## 2. workflow-master 처리

- target repo(`virtue-rebirth-app`)·Infinity 모두에 `.agent/workflows/workflow-master.md` · `WORKFLOW-MASTER.md` **부재**(`find` 무매치).
- 부재를 문서 전제/본 보고서에 기록하고, 선행 Virtue Intent(marketing-16/17/18)와 동일하게 **Planner/Developer/Marketer/Operator 4역할 병렬 합성** 규율(scope→risk→plan→verify)로 진행. Developer는 read-only로 `/` 홈 코드 ground truth(file:line)를 잡고, Planner는 선행 문서 일관성·범위 차별화, Marketer는 FAE 프레이밍·세 질문 평가, Operator는 검증 게이트·안전 push 설계를 담당. 합성·단일 파일 작성은 중앙(workflow-master)에서 수행해 파일 충돌을 차단했다.

## 3. 변경 파일 / 커밋 / push

| 항목 | 값 |
|---|---|
| repo | `virtue-rebirth-app` |
| branch | `master` (정상 origin 브랜치) |
| 신규 파일 | `apps/web/docs/home-screen-fae-audit.md` (1파일) |
| commit | `3d90648` (이전 HEAD `f74cf59` fast-forward) |
| push | `f74cf59..3d90648  master -> master` 성공 |
| 동기화 | local HEAD `3d90648` == `origin/master` `3d90648`, 워킹트리 clean |

Infinity:

| 항목 | 값 |
|---|---|
| repo | `infinity` (knowledge-lab 서브모듈) |
| branch | `main` |
| 신규/변경 | `reports/marketing-19/2026-05-26T1007Z-local.md`(신규), `INTENTS.md`(Inbox→Archive), `intents/archive/marketing-19.md`(신규) |
| 제외 | `EVALUATION_NOTES.md`(무관 dirty 파일 — 미스테이징·미커밋, 그대로 보존) |

## 4. 검증 게이트 결과

| 게이트 | 결과 |
|---|---|
| Gate A 충돌 마커 `git grep -nE '^(<<<<<<< \|=======$\|>>>>>>> )'` + 신규 파일 직접 grep | PASS (양쪽 "clean", 라인시작 실제 마커 0; 문서 §6 self-check 명령은 `git grep` 접두라 `^` 미매치) |
| Gate B 변경 스코프 `git status --porcelain` | PASS (`?? apps/web/docs/home-screen-fae-audit.md` 1줄만) |
| Gate B 추적 파일 수정 `git diff --stat` | PASS (빈 출력) |
| Gate C 금지 경로 tripwire(`apps/web/src`·`apps/ios`·`public`·`next.config`·`layout.tsx`·`route.ts`·`.env`·`posthog`·`analytics`·`telemetry`·`schema`·`.github`·`Dockerfile`·`package.json`) | PASS ("clean" — 0 매치) |
| Gate D Infinity 선택적 스테이징, `EVALUATION_NOTES.md` 인덱스 제외 | PASS (staged = report+INTENTS+archive만, `EVALUATION_NOTES.md` 미스테이징 확인) |
| Gate E post-push 동기화 | PASS (Virtue/Infinity 양쪽 HEAD==origin, Infinity는 push 후에도 `M EVALUATION_NOTES.md`만 잔존) |
| 선행 문서 충돌 | 0 (§6: three-screen·empty-state·friction-protocol·activation-ladder·seven-day-loop·jtbd-matrix·first-impression·copy-spec) |
| 코드/카피/이벤트/대시보드/배포 변경 | 0건 |

## 5. L2 agent-approved push 기록 (필수 항목)

### Virtue push
| 항목 | 내용 |
|---|---|
| 승인 | `agent-approved L2` — `apps/web/docs/` 문서 1파일 추가로 한정된 doc-only 변경. Intent permission(L1 내부 문서/감사표)과 정합. |
| 영향(impact) | 내부 문서 1개 추가. 런타임/사용자 노출/공개 표면 영향 0. |
| 가역성(reversibility) | 높음 — 단일 신규 파일, `git revert 3d90648`로 즉시 복구. 다른 파일 미변경. |
| 비용(cost) | 0 — 빌드·배포·외부 호출·구매 없음. |
| production/secrets/permissions | 변경 0 — 배포·CI 트리거 없음, 시크릿/권한/env 미접근. |
| 제3자 메시징 위험 | 없음 — 외부 발송/제출/DM/메일/API mutate 0. 배경 출처 링크 외부 fetch도 안 함. |
| 검증 결과 | §4 Gate A~C, E PASS. push fast-forward, HEAD==origin/master. |

### Infinity push
| 항목 | 내용 |
|---|---|
| 승인 | `agent-approved L2` — 보고서 1파일 + 레지스트리(INTENTS Inbox→Archive, archive 파일 1) 추가로 한정. |
| 영향(impact) | Infinity 레지스트리/보고서만. 배포 산출물·외부 노출 0. |
| 가역성(reversibility) | 높음 — `git revert`. 무관 dirty 파일 `EVALUATION_NOTES.md` 미접근. |
| 비용(cost) | 0. |
| production/secrets/permissions | 변경 0. |
| 제3자 메시징 위험 | 없음. |
| 검증 결과 | §4 Gate A, D, E PASS. 선택적 staging으로 `EVALUATION_NOTES.md` 인덱스 제외 확인. push fast-forward, HEAD==origin/main. push 후 `git status`에 `M EVALUATION_NOTES.md`만 잔존(의도된 무관 변경). |

## 6. Infinity 레지스트리 처리

- `INTENTS.md`: marketing-19를 Inbox에서 제거하고 Archive에 완료 HTML 코멘트 1줄 추가.
- `intents/archive/marketing-19.md` 신규 작성(선행 archive 파일 포맷 계승).
- **무관 dirty 파일 `EVALUATION_NOTES.md`는 건드리지 않았다**(스테이징/커밋 제외, 그대로 보존). `git add -A` 미사용, 경로 명시 스테이징만 사용.

## 7. 범위 밖 / 후속

- **부모 knowledge-lab 서브모듈 포인터 미갱신:** `infinity`는 `knowledge-lab`의 서브모듈이지만, 본 Intent는 `infinity` repo push만 인가했고 부모에는 무관한 dirty 상태가 있어 부모 포인터 갱신은 하지 않았다(out of scope). 필요 시 별도 처리.
- 갭 G1~G5를 메울 카피·UI 후보는 Waiting/approval-needed. 공개 카피 반영·배포는 본 Intent 금지선.

## 8. 블로커

- 없음. 양쪽 repo 모두 정상 fast-forward push, 충돌/rebase 없음.
