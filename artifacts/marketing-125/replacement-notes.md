# marketing-125 replacement notes

## marketing-124에서 확인된 실패

- 근사 원호가 깨끗한 원으로 읽히지 않음
- 3분할이 균등하지 않음
- 화살표 머리를 손으로 배치해 접선 방향이 어긋남
- Card 2 텍스트가 링과 충돌함
- 분야 키워드만 나열해 반복 실험의 내용이 보이지 않음
- Red가 렌더 PNG를 직접 보지 않음

## 이번 재작업의 변경

- 단일 중심 `(540, 650)`과 반지름 `260`에서 모든 좌표를 파생했다.
- 시작각 `0°, 120°, 240°`와 각 구간 `120°`로 같은 원을 세 번 그렸다.
- SVG `A 260 260 0 0 1` arc를 사용하고, 모든 호의 선폭을 `32px`로 고정했다.
- 화살표 tip과 base를 종료점의 접선 `(-sin θ, cos θ)`과 법선에서 계산했다. tip은 arc endpoint와 동일하다.
- 제목은 y=174~306, 링은 y=390~910, 설명은 y=990 이후로 분리해 텍스트 안전영역을 확보했다.
- 카드 메시지를 `실험 → 증거 → 다음 수정`으로 분리했다.
- Chromium headless로 SVG를 실제 1080×1350 PNG로 렌더하고, PNG를 직접 확인하는 Red 게이트를 추가한다.

## 공개 경계

`public_posted: false`, `profile_changed: false`, `external_uploaded: false`. 사용자 승인 전 공개 게시·프로필 변경·외부 업로드는 실행하지 않는다.
