# ops-23 KL ingest/source migration audit

- 실행 시각: 2026-08-09T21:58Z
- 대상: `/home/ubuntu/workspace/knowledge-lab/source/openclaw-system/`
- 범위: source 경로 참조, daily-tracking 색인, runtime/cache/secret/media 경계, cron·Infinity·Genie 협업 문서

## Planner

- 판단: 요청은 새 기능 구현이 아니라 구조 이동 이후의 재현 가능한 감사 산출물을 만드는 audit 태스크다.
- 완료 기준: canonical/legacy 경로를 구분하고, meaningful daily-tracking의 wiki 연결 여부를 확인하며, ignored runtime 자료가 ingest에서 누락되거나 정본을 오염시키는지 확인하고, 후속 액션을 report로 남긴다.
- Knowledge Lab 근거: `agent-wiki/README.md`의 source 계층·관찰 가능성 원칙, `source/openclaw-system/README.md`의 ingest 및 runtime 제외 규칙, `agent-wiki/content/docs/diary/2026-08-09.mdx`와 `content/docs/log.mdx`의 최근 이동 기록.

## Developer

- 확인: `/home/ubuntu/.openclaw/workspace/system`은 canonical source로 symlink되어 호환성 경로는 살아 있다.
- 확인: source README는 `docs/`, `data/daily-tracking/`을 ingest 입력으로, `cache/`, `trash/`, `tmp/`, virtualenv/Python cache를 runtime-only로 규정한다. 실제 git status에도 해당 runtime 디렉터리들이 ignored로 나타난다.
- 결함: tracked card-news template/library가 ignored `cache/`, `generated-assets/`, `source-assets/`, `reports/.../runs/`를 직접 참조하는 사례가 남아 있다. 깨끗한 checkout에서 재현성이 깨질 수 있다.
- 결함: `docs/LIFE_SYSTEM_WORKFLOW_ARTIFACT.md:229`는 `/home/ubuntu/workspace/system-dashboard/...`를 참조한다. 이는 openclaw-system 이동 경로는 아니지만, 정본 경계 점검 시 별도 외부 의존성으로 표시해야 한다.

## Marketer

- 사용자-facing 영향: raw daily-tracking을 모두 wiki로 복제하면 노이즈가 커지므로, 재사용 판단을 바꾸는 날만 얇은 diary/log pointer를 둔다는 현재 규칙은 적합하다.
- 결함: 의미 있는 여행·제품·콘텐츠 관찰이 있는 2026-06-06, 2026-07-02, 2026-07-17, 2026-07-20, 2026-07-29, 2026-08-05 등은 같은 날짜의 명시적 diary pointer가 확인되지 않아 재사용성이 낮다. 2026-08-09는 정상 색인됐다.
- 제안: 과거 캡처를 장문 wiki로 복사하지 말고, 각 날짜별 1~3줄의 재사용 키워드·다음 판단·원문 경로만 backfill한다.

## Operator

- 확인: migrated cron snapshot의 22개 payload 중 10개가 `/home/ubuntu/.openclaw/workspace/system` legacy path를 사용하고, canonical `source/openclaw-system`을 직접 참조하는 payload는 0개다. symlink로 당장은 작동하지만 이동 후 문서·운영 표면의 정합성은 낮다.
- 한계: 현재 로컬 cron snapshot에는 이번 dispatcher id `a409109a-b1ff-4242-9f17-1eb68e5880a0`가 없어 실제 활성 payload의 보존 여부를 파일만으로 검증할 수 없다. 런타임 cron 조회가 필요한 관측성 공백이다.
- 비용/권한: 이번 감사는 로컬 read-only와 Infinity 원장/report 작성만 수행했으며, 외부 발송·배포·비용·시크릿·권한 변경은 하지 않았다.

## Workflow-master synthesis

- 합의: source README와 recent wiki 기록은 새 경계를 설명하고, symlink는 단기 호환성을 제공한다.
- 충돌: raw capture를 전부 wiki에 올리자는 방향은 Knowledge Lab의 노이즈 억제 원칙과 충돌하므로 기각했다.
- 채택 순서: (1) cron canonical path/메타데이터 관측성 보강, (2) meaningful daily-tracking 얇은 pointer backfill, (3) ignored asset을 참조하는 publish/reproducibility gate 보강.
- 기각안: 전체 daily-tracking의 장문 diary 복제, cache/trash를 ingest 대상으로 승격, legacy symlink 즉시 제거.

## Red validation

- `red_status: pass`
- 검증 근거: 요청된 네 감사 축을 모두 다뤘고, 각 발견에 파일·수량·경계를 연결했으며, 다음 액션을 별도 후속 intent로 분리할 수 있게 했다.
- report: `reports/ops-23/20260809T2158Z.html`

## Next action

새 후속 intent에서만 수정한다: `daily-tracking` pointer backfill, active cron payload canonicalization/dispatcher metadata 확인, ignored asset provenance gate 추가.
